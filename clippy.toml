# Clippy configuration for BitQuan
# Enforce strict safety checks in production code

# Disallow unwrap() and expect() in production code
disallowed-methods = [
    "unwrap",
    "expect"
]

# Allow in test code
allow-unwrap-in-tests = true
allow-expect-in-tests = true