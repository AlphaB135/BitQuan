name: Integration Tests

on:
  push:
    branches: [ main, develop, feature/*, fix/* ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    - cron: '0 4 * * 0'  # Weekly at 4 AM UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Basic integration tests
  basic-integration:
    name: Basic Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config clang

      - name: Install test tools
        run: |
          cargo install cargo-nextest --locked
          cargo install cargo-watch --locked

      - name: Run basic integration tests
        run: |
          BITQUAN_SKIP_PQC_TESTS=1 cargo nextest run --workspace --test integration \
            --features="full" \
            --env-file .env.test \
            --max-failures 1

      - name: Test node startup
        run: |
          cargo test --test node_startup --release

      - name: Test basic RPC calls
        run: |
          cargo test --test rpc_basic --release

      - name: Test transaction flow
        run: |
          cargo test --test transaction_flow --release

  # Network integration tests
  network-integration:
    name: Network Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        scenario: [sync, peers, broadcast]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config

      - name: Run network integration tests
        run: |
          case ${{ matrix.scenario }} in
            sync)
              cargo test --test network_sync --release -- --nocapture
              ;;
            peers)
              cargo test --test peer_management --release -- --nocapture
              ;;
            broadcast)
              cargo test --test block_broadcast --release -- --nocapture
              ;;
          esac

  # Multi-node integration tests
  multi-node:
    name: Multi-Node Integration
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config docker

      - name: Start test cluster
        run: |
          # Create a multi-node test setup
          mkdir -p test-cluster/{node1,node2,node3}

          # Generate configs
          for i in 1 2 3; do
            cp config/testnet.toml test-cluster/node$i/config.toml
            sed -i "s/port = 8333/port = $((8330 + i))/g" test-cluster/node$i/config.toml
            sed -i "s/rpc_port = 8334/rpc_port = $((8340 + i))/g" test-cluster/node$i/config.toml
          done

          # Start nodes in background
          for i in 1 2 3; do
            timeout 60s cargo run --release -- \
              --config test-cluster/node$i/config.toml \
              --data-dir test-cluster/node$i/data \
              --peers node1:8331,node2:8332,node3:8333 &
          done

          # Wait for nodes to start
          sleep 30

          # Test connectivity
          curl -f http://localhost:8331/health || exit 1
          curl -f http://localhost:8332/health || exit 1
          curl -f http://localhost:8333/health || exit 1

          # Test RPC calls
          curl -f http://localhost:8331/rpc/getblockcount || exit 1
          curl -f http://localhost:8332/rpc/getblockcount || exit 1
          curl -f http://localhost:8333/rpc/getblockcount || exit 1

      - name: Test consensus
        run: |
          # Test that nodes reach consensus
          for i in 1 2 3; do
            height=$(curl -s http://localhost:$((8340 + i))/rpc/getblockcount | jq -r '.result')
            echo "Node $i height: $height"
          done

      - name: Clean up
        run: |
          # Kill background processes
          pkill -f "bitquan-node" || true

          # Remove test data
          rm -rf test-cluster

  # Database integration tests
  database-integration:
    name: Database Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install database dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            postgresql-client \
            redis-tools \
            sqlite3 \
            libsqlite3-dev

          # Start PostgreSQL
          sudo service postgresql start
          sudo -u postgres createdb bitquan_test

          # Start Redis
          sudo service redis-server start

      - name: Test PostgreSQL integration
        run: |
          cargo test --test postgres_integration --release -- --nocapture

      - name: Test Redis integration
        run: |
          cargo test --test redis_integration --release -- --nocapture

      - name: Test RocksDB integration
        run: |
          cargo test --test rocksdb_integration --release -- --nocapture

  # Wallet integration tests
  wallet-integration:
    name: Wallet Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config

      - name: Test wallet creation
        run: |
          cargo test --test wallet_create --release -- --nocapture

      - name: Test wallet operations
        run: |
          cargo test --test wallet_ops --release -- --nocapture

      - name: Test transaction signing
        run: |
          cargo test --test tx_signing --release -- --nocapture

      - name: Test wallet backup
        run: |
          cargo test --test wallet_backup --release -- --nocapture

  # Performance integration tests
  perf-integration:
    name: Performance Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config

      - name: Install perf tools
        run: |
          cargo install cargo-flamegraph --locked
          cargo install cargo-miri

      - name: Test block processing performance
        run: |
          cargo test --test block_perf --release -- --nocapture

      - name: Test mempool performance
        run: |
          cargo test --test mempool_perf --release -- --nocapture

      - name: Test RPC performance
        run: |
          cargo test --test rpc_perf --release -- --nocapture

  # Stress tests
  stress-tests:
    name: Stress Tests
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config

      - name: Run stress tests
        run: |
          # High load scenario
          for i in {1..1000}; do
            curl -s http://localhost:8334/rpc/getblockcount > /dev/null &
          done

          # Wait for all requests to complete
          wait

          # Memory stress test
          timeout 60s cargo test --test memory_stress --release || true

      - name: Test under load
        run: |
          # Simulate network load
          for i in {1..50}; do
            curl -s http://localhost:8334/rpc/getblockheight > /dev/null &
            curl -s http://localhost:8334/rpc/getmempoolinfo > /dev/null &
          done

          # Wait and check system status
          sleep 30
          echo "Load test completed"

  # Security integration tests
  security-integration:
    name: Security Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config

      - name: Test authentication
        run: |
          cargo test --test auth_tests --release -- --nocapture

      - name: Test API rate limiting
        run: |
          cargo test --test rate_limiting --release -- --nocapture

      - name: Test input validation
        run: |
          cargo test --test input_validation --release -- --nocapture

      - name: Test error handling
        run: |
          cargo test --test error_handling --release -- --nocapture

  # Results aggregation
  aggregate-results:
    name: Aggregate Test Results
    runs-on: ubuntu-latest
    needs: [basic-integration, network-integration, multi-node, database-integration, wallet-integration, perf-integration, stress-tests, security-integration]
    if: always()

    steps:
      - name: Generate test summary
        run: |
          echo "## Integration Test Summary" > test-summary.md
          echo "" >> test-summary.md
          echo "- Basic Integration: ${{ needs.basic-integration.result }}" >> test-summary.md
          echo "- Network Integration: ${{ needs.network-integration.result }}" >> test-summary.md
          echo "- Multi-Node: ${{ needs.multi-node.result }}" >> test-summary.md
          echo "- Database Integration: ${{ needs.database-integration.result }}" >> test-summary.md
          echo "- Wallet Integration: ${{ needs.wallet-integration.result }}" >> test-summary.md
          echo "- Performance Integration: ${{ needs.perf-integration.result }}" >> test-summary.md
          echo "- Stress Tests: ${{ needs.stress-tests.result }}" >> test-summary.md
          echo "- Security Integration: ${{ needs.security-integration.result }}" >> test-summary.md
          echo "" >> test-summary.md
          echo "**Overall Status: ${{ job.status }}**" >> test-summary.md

      - name: Upload test summary
        uses: actions/upload-artifact@v4
        with:
          name: test-summary-${{ github.run_number }}
          path: test-summary.md
          retention-days: 30

      - name: Update test results
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const summary = fs.readFileSync('test-summary.md', 'utf8');
            console.log(summary);

            // Update PR with test results
            if (context.eventName === 'pull_request') {
              await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: 'Integration Test Results:\n' + summary
              });
            }