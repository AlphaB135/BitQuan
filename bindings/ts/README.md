# BitQuan TypeScript Bindings

Scope:
- Address encode/decode and PSBT(PQC) assembly for web/Node.js.
- Lightweight RPC client for node integration.

Layout proposal:
- package: `@bitquan/sdk` (mono-repo subpkg), built with TypeScript + vitest.

Next steps:
- [ ] Define minimal API (encode/decode, PSBT fields, tx serialize).
- [ ] Set up package scaffolding and CI after API review.
